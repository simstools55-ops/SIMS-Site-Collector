# RC10.2 Hotfix

## UI
`追加の操作 → 収集状況を確認` はダイアログを開きません。
現在のRunを収集状況シートへ再描画し、そのシートを表示します。

収集開始時の保存画面は利用者の明示操作で開き、その画面内で進捗をポーリング表示します。
時間トリガーはRun/シート状態だけを更新します。

## Evidence Package名
新規Runでは必ず次の形式を使用します。

`SIMS-Evidence-{サイト名}-{yyyyMMdd-HHmm}.zip`

例:
`SIMS-Evidence-スマホ生活-20260815-1507.zip`

古い `config.outputFileName` や旧Runのファイル名を新規Runへ引き継ぎません。
