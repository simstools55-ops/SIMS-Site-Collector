# RC3 Apply

Changed Apps Script files only:
- Code.gs — replace
- EvidencePackager.gs — replace

All other .gs files are unchanged from RC2.

To recover the current RC2 run:
1. Replace the two files.
2. Save and reload the spreadsheet.
3. Choose `4. Resume Collection` once.
4. Choose `5. Show Status`; it should report COMPLETED and preserve the existing Output URL.
